services:
  price-scraper:
    build: .
    container_name: price-scraper-app
    ports:
      - "8000:8000"
    volumes:
      # This saves stores.json to the user's actual hard drive so data persists
      - ./stores.json:/app/stores.json
    restart: unless-stopped